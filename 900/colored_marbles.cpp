#include <iostream>
#include <stdlib.h>
#include <vector>

using namespace std;

int main()
{
    int t, n;
    cin >> t;

    for (int counter = 0; counter < t; counter++)
    {
        cin >> n;
        vector<int> marbles(n);
        for (int &value : marbles)
        {
            cin >> value;
        }

        
    }
}