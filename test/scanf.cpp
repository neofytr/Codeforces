#include <bits/stdc++.h>

using namespace std;

int main()
{
    int N;
    scanf("%d\n", &N);

    while (N--)
    {
        char x[110];
        scanf("0.%[0-9]...\n", x);

        printf("the digits are: 0.%s\n", x);
    }

    return 0;
}