#include <bits/stdc++.h>
using namespace std;

#define int long long

void solve() {
	int n; cin >> n;

	vector<int> a(2 * n + 1, 0);
	for (int r = 1; r <= 2 * n; r++) cin >> a[r];
}

int32_t main() {
	int t;
	cin >> t;
	while(t--) solve();
	return 0;
}