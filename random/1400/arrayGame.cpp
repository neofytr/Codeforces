#include <bits/stdc++.h>
using namespace std;

#define int long long
#define MAX (int)(2 * 1e3)

int arr[MAX + 1];
void solve() {
	int n, k;
	cin >> n >> k;

	for (int r = 1; r <= n; r++) cin >> arr[r];
}

int32_t main() {
	int t;
	cin >> t;

	while (t--) {
		solve();
	}
	return 0;
}