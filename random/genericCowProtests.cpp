#include <bits/stdc++.h>
using namespace std;

#define int long long
#define MOD (1e9 + 9)

int32_t main() {
	int n; cin >> n;
	vector<int> arr(n + 1);
	for (int r = 1; r <= n; r++) cin >> arr[r];

	set<pair<int, int>> 
}