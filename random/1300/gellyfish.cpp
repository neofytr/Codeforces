#include <bits/stdc++.h>
using namespace std;

#define int long long 
#define MOD (998_244_353)

void solve() {
	int n;
	cin >> n;

	vector<int> p(n), q(n);
	for (int r = 0; r < n; r++) cin >> p[r];
	for (int r = 0; r < n; r++) cin >> q[r];


}

int32_t main() {
	int t;
	cin >> t;

	while (t--) {
		solve();
	}
	return 0;
}