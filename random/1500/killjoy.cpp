#include <bits/stdc++.h>
using namespace std;

#define int long long

void solve() {
	int n, x; cin >> n >> x;
	vector<int> r(n + 1);
	for (int i = 1; i <= n; i++) cin >> r[i];
}

int32_t main() {
	int t; cin >> t;
	while (t--) solve();
	return 0;
}