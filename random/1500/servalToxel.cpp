#include <bits/stdc++.h>
using namespace std;

#define int long long

void solve() {
	int n, m; cin >> n >> m;
	vector<int> a(n + 1);
	for (int i = 1; i <= n; i++) cin >> a[i];

	int p, v;
	while (m--) {
		cin >> p >> v;
	}
}

int32_t main() {
	int t; cin >> t;
	while (t--) solve();
	return 0;
}