#include <bits/stdc++.h>
using namespace std;

#define int long long

void solve() {
	int h, n; cin >> h >> n;
	vector<int> a(n + 1), c(n + 1);
	for (int r = 1; r <= n; r++) cin >> a[r];
	for (int r = 1; r <= n; r++) cin >> c[r];

	
}

int32_t main() {
	int t; cin >> t;
	while (t--) solve();
	return 0;
}