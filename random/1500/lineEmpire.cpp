#include <bits/stdc++.h>
using namespace std;

#define int long long

int dp[1000][1000];

void solve() {
	int n, a, b; cin >> n >> a >> b;
	vector<int> x(n + 1);
	for (int r = 1; r <= n; r++) cin >> x[r];

	
}

int32_t main() {
	int t; cin >> t;
	while (t--) solve();
	return 0;
}