#include <bits/stdc++.h>
using namespace std;

#define int long long

int32_t main() {
	int q; cin >> q;
	while (q--) {
		string t; cin >> t;

		if (t == "+") {
			int x; cin >> x;

		
		} else {
			int k; cin >> k;
		}
	}

	return 0;
}