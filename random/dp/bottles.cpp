#include <bits/stdc++.h>
using namespace std;

#define int long long 
#define MAX (int)(1e2)

int A[MAX + 1], B[MAX + 1];

int32_t main() {
	int n;
	cin >> n;

	for (int r = 1; r <= n; r++) cin >> A[r];
	for (int r = 1; r <= n; r++) cin >> B[r];
	return 0;
}