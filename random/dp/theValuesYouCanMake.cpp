#include <bits/stdc++.h>
using namespace std;

#define int long long 
#define MAX (int)(5 * 1e2)

int c[MAX + 1];

int32_t main() {
	int n, k;
	cin >> n >> k;

	for (int r = 1; r <= n; r++)
		cin >> c[r];
	return 0;
}