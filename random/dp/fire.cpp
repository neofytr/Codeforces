#include <bits/stdc++.h>
using namespace std;

#define int long long 
#define MAX (int)(1e2)

int t[MAX + 1], b[MAX + 1], v[MAX + 1];
int32_t main() {
	int n;
	cin >> n;

	for (int r = 1; r <= n; r++)
		cin >> t[r] >> b[r] >> v[r];
	return 0;
}