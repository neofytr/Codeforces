#include <bits/stdc++.h>
using namespace std;

#define int long long

int32_t main() {
    int m, s;
    cin >> m >> s;

    if (!s && m > 1) {
        cout << -1 << " " << -1 << endl;
        return 0;
    }

    if (!s && m == 1) {
        cout << 0 << " " << 0 << endl;
        return 0;
    }

    
}