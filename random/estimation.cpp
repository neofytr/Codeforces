#include <bits/stdc++.h>
using namespace std;

#define int long long
#define MAX (int)(2 * 1e3)
#define MAX_K (int)(25)

int32_t main() {
	int n, k; cin >> n >> k;
	vector<int> arr(n + 1);
	for (int r = 1; r <= n; r++) cin >> arr[r];
}