#include <bits/stdc++.h>

int main() {
    std::ios_base::sync_with_stdio(false);
    std::cin.tie(NULL);
    std::cout.tie(NULL);
    long long n;
    std::cin >> n;
    long long result = n + n / 2;
    std::cout << result << std::endl;
    return 0;
}