#include <bits/stdc++.h>
using namespace std;

#define int long long

void solve() {
	int n; cin >> n;
	multiset<int> ms;
	vector<int> a(n);
	for (int i = 1; i <= n - 1; i++) cin >> a[i];
}

int32_t main() {
	int t; cin >> t;
	while (t--) solve();
	return 0;
}