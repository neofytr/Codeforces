#include <bits/stdc++.h>
using namespace std;

#define int long long
#define MOD (int)(1e9 + 7)

int32_t main() {
	string s; cin >> s;
	int k; cin >> k;
	vector<string> dict(k);
	for (string &s : dict) cin >> s;

	return 0;
}