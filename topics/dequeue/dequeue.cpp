#include <bits/stdc++.h>
using namespace std;

int main() {
    // STL dequeue is a sequential container that provides the functionality of a double ended queue
    // In a regular queue, elements are added from the rear and removed from the front
    // However, in a dequeue, we can insert and remove elements from both the front and the rear
}
