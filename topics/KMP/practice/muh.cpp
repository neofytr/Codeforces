#include <bits/stdc++.h>
using namespace std;

#define int long long

int32_t main() {
	int n, w; cin >> n >> w;
	vector<int> a(n + 1), b(w + 1);
	for (int i = 1; i <= n; i++)
		cin >> a[i];
	for (int i = 1; i <= w; i++)
		cin >> b[i];

	map<int, int> f;
	return 0;
}
