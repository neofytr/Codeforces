#include <bits/stdc++.h>
using namespace std;

#define int long long

int32_t main() {

}

we have to put A3 on A4 when we print the cheroku (there is a tool for that); we can get exact dimensions too;
the view is 400 by 300 (length 400 mm, widht 300 mm); we need to fit the cheroku in there but not the whole
view, i should be able to configure the size