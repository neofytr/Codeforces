#include <bits/stdc++.h>
using namespace std;

#define int long long 
#define MAX (int)(5 * 1e5)
#define OFFSET MAX

int str[MAX + 1];
int cnt[MAX * 2 + 1];
int32_t main() {
	int n;
	cin >> n;

	string s;
	cin >> s;

	return 0;
}