#include <bits/stdc++.h>
using namespace std;

#define int long long

// Problem: twoDiv
// URL: https://codeforces.com/problemset/problem/1916/B
// Difficulty: easy
// Tags:
// Strategy:

int32_t main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int t;
    cin >> t;
    while (t--) {
        int a, b;
        cin >> a >> b;
        // 1 <= a < b < x
        // a and b are the two largest divisors of a number x
        // find x
    }

    return 0;
}